// import { api } from "@/lib/api";
// import { ResumeFeedbackResponse } from "./resumeInterface";

// export const getResumeFeedback = async (resumeId: number, jobId: number): Promise<ResumeFeedbackResponse> => {
//   const { data } = await api.get(`/api/user/me/resume/feedback/${resumeId}/job/${jobId}`);
//   return data;
// };




