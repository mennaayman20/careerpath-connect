export interface ResumeResponse {
  id: number;
  fileName: string;
  createdAt: string;
  blobName: string;
}

export interface ResumeUploadResponse {
  id: number;
  fileName: string;
  createdAt: string;
  blobName: string;
}